act main() => int {
    mark x:int = 0;
    
    scan (mark y:int = 20; y>10; y--){
        if (y == 15){
            break;
        }
        printf("y = %i\n", y);
        x = y;
    }
    return x;
}